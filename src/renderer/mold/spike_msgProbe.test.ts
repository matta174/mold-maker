// Throwaway probe — findings captured in spike_meshToStep.test.ts.
// Kept as a skipped no-op because the sandbox cannot delete files.

import { describe, it } from 'vitest';

describe.skip('PROBE: Message-related exports (superseded)', () => {
  it('superseded', () => { /* no-op */ });
});
