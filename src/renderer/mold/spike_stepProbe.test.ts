// Superseded by spike_stepExport.test.ts + spike_stepFilename.test.ts.
// Kept as an empty no-op because the sandbox cannot delete it, but nothing
// here exercises anything useful. Safe to remove on next repo cleanup.

import { describe, it } from 'vitest';

describe.skip('PROBE v2: STEPControl_Writer Write path (superseded)', () => {
  it('superseded — see spike_stepFilename.test.ts for filename diagnostics', () => {
    // no-op
  });
});
