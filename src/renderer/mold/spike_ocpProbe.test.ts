// Throwaway probe — already consumed. Findings are captured in
// spike_meshToStep.test.ts comments. Kept as a skipped no-op because the
// sandbox cannot delete files.

import { describe, it } from 'vitest';

describe.skip('PROBE: BRepBuilderAPI overload signatures (superseded)', () => {
  it('superseded', () => { /* no-op */ });
});
